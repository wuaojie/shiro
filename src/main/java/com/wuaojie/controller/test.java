package com.wuaojie.controller;

import java.util.Collections;
import java.util.HashMap;
import java.util.Map;
import java.util.Set;

public class test {
    public static void main(String[] args) {
//        Set<K> keys = this.map.keySet();
        //如果keys有值则返回Collections.unmodifiableSet(keys)--这个是不可修改的。
        //没值返回Collections.emptySet()--这个是返回一个空的集合。适用与你没值返回new一个集合，但是
        //new对象会消耗内存
//        return !keys.isEmpty() ? Collections.unmodifiableSet(keys) : Collections.emptySet();


    }

}
